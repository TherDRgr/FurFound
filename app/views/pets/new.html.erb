<div class="container mt-5">
  <h1 class="text-center mb-4">Add New Pet</h1>

  <%= render "form", pet: @pet %>

  <div class="text-center mt-4">
    <%= link_to "Back to pets", pets_path, class: 'btn btn-secondary' %>
  </div>
</div>
